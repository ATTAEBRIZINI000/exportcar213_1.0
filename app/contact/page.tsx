import ContactClient from './ContactClient'

export const metadata = {
  title: 'Nous Contacter — Export Car 213',
  description: 'Contactez Export Car 213 — Nanterre (92) et Caen (14). Formulaire, WhatsApp, téléphone et carte.',
}

export default function ContactPage() {
  return <ContactClient />
}
